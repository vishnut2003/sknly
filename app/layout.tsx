import type { Metadata } from "next";
import { Plus_Jakarta_Sans } from "next/font/google";
import localFont from "next/font/local";
import "./globals.css";
import ReduxProvider from "@/providers/redux-provider";
import NextAuthProvider from "@/providers/nextauth-provider";

const plusJakartaSans = Plus_Jakarta_Sans();

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const glamourAbsolute = localFont({
  src: "../fonts/glamour_absolute_regular.ttf",
  variable: "--font-glamour-absolute",
  display: "swap",
})

const sloopFont = localFont({
  src: "../fonts/sloops.ttf",
  variable: "--font-sloops",
  display: "swap",
})

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${plusJakartaSans.className} ${glamourAbsolute.variable} ${sloopFont.variable} antialiased`}
      >
        <ReduxProvider>
          <NextAuthProvider>
            {children}
          </NextAuthProvider>
        </ReduxProvider>
      </body>
    </html>
  );
}
